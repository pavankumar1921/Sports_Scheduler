<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="./css/styles.css" rel="stylesheet">
    <style>
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            width:100vw;
        }

        .form-container {
            max-width: 400px;
            width:100%;
            padding: 40px;
            border: 1px solid #050505;
            border-radius: 8px;
        }
    </style>
    <title><%= title %></title>
</head>
<body class="bg-gray-100">
    <div class="fixed top-0 w-full z-10">
        <%-include("header.ejs")%>
    </div>
    <div class="container justify-start pt-16 px-1 py-2">
        <a href="/session/<%=sessionId%>" class="text-white bg-blue-500 hover:bg-blue-800 font-medium rounded px-3 py-1.5">Back</a>
    </div>
    <div class="px-8 mt-20">
        <div class="container max-w-sm mx-auto">
            <div class="form-container bg-green-100">
                <div class="py-4">
                    <p class="font-semibold text-2xl text-green-600">Cancel Session</p>
                </div>
                
            <form action="/session/<%=sessionId%>/cancelSession" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken%>">
                <div class="py-3">
                    <label for="cancel" >What is the reason you are cancelling the session?</label>
                    <input id="cancel" name="cancel" type="text" placeholder="Reason" class="border border-gray-300 text-gray-900 rounded p-2 w-full" required>
                </div>
                <div class="py-3">
                    <button type="submit" class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2">Cancel</button>
                </div>
            </form>
        </div>
        <%-include ("flashMessages.ejs") %>
    </div>  
</body>
</html>